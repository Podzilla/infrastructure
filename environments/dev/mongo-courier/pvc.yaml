apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongo-courier-pvc
  namespace: dev
  labels:
    app: mongo-courier
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi