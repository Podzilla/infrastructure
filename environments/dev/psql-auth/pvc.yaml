apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: psql-auth-pvc
  namespace: dev
  labels:
    app: psql-auth
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi