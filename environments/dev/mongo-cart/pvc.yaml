apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongo-cart-pvc
  namespace: dev
  labels:
    app: mongo-cart
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi