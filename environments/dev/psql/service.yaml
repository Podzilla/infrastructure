apiVersion: v1
kind: Service
metadata:
  name: psql-service
  namespace: dev
  labels:
    app: psql
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
      name: postgres
  selector:
    app: psql